<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_test">
        <activity
            android:name=".AdventureCreateActivity"
            android:exported="true"
            android:theme="@style/Theme.LoginScreen"/>
        <activity
            android:name=".adventures.adventure_game_one.AdventureGameActivity"
            android:theme="@style/Theme.LoginScreen" />
        <activity
            android:name=".SignupActivity"
            android:exported="true"
            android:theme="@style/Theme.LoginScreen" /> <!-- TODO: update themes of activities here. -->
        <activity
            android:name=".adventures.ScenarioInitActivity"
            android:exported="true" />
        <activity
            android:name=".AdventureSelectActivity"
            android:exported="true"
            android:theme="@style/Theme.LoginScreen" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>